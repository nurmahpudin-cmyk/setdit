backend/node_modules/*
backend/.wwebjs_auth/*
backend/.wwebjs_cache/*
backend/.env
frontend/node_modules/*
backend/add_menus.sql
frontend/dist/*
backend/uploads/*
backend/dist/*
tmp-nginx/*
# Postgres data (bind mount)
postgres-data/

# DB dump (transfer manual ke server, jangan commit)
db-init/*.sql

# TLS cert (self-signed, generate ulang per environment / ganti dgn Lets Encrypt di server asli)
frontend/certs/
